<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= profile.username %>'s Profile - New Calendar</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <nav>
      <div class="logo">New Calendar</div>
      <ul>
        <li><a href="/">Home</a></li>
        <% if (locals.user) { %>
          <li><a href="/user/dashboard">Dashboard</a></li>
          <li><a href="/user/profile">Your Profile</a></li>
          <li><a href="/auth/logout">Logout</a></li>
        <% } else { %>
          <li><a href="/auth/login">Login</a></li>
          <li><a href="/auth/register">Register</a></li>
        <% } %>
      </ul>
    </nav>
  </header>
  
  <main class="container">
    <section class="public-profile">
      <h1><%= profile.username %>'s Profile</h1>
      
      <div class="profile-details">
        <div class="profile-item">
          <h3>Username</h3>
          <p><%= profile.username %></p>
        </div>
        
        <div class="profile-item">
          <h3>Member Since</h3>
          <p><%= profile.createdAt.toLocaleDateString() %></p>
        </div>
      </div>
    </section>
    
    <% if (process.env.NODE_ENV !== 'production') { %>
      <div class="debug-info">
        <h3>Debug Information:</h3>
        <p>Profile view accessed at: <%= new Date().toISOString() %></p>
        <p>Username parameter: <%= profile.username %></p>
      </div>
    <% } %>
  </main>
  
  <footer>
    <p>&copy; 2025 New Calendar. All rights reserved.</p>
  </footer>
</body>
</html>
